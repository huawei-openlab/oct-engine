caseschedular -- get cases from tcserver and provide an UI for user to start/read/load case
  -- The use can see it

scheduler -- get test job from case-manager and deliver to os/container 
  -- The use cannot see it
